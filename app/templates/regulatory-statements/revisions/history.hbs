{{page-title "History"}}
<DocumentHistory 
  @documentContainerId={{this.model.documentContainer.id}}
  @currentVersion={{this.model.currentVersion}}
  @currentRevisionId={{this.model.editorDocument.id}}
/>
{{outlet}}