<button {{on "click" this.login}} type="submit" class="{{@styles}}">
  {{yield}}
</button>
{{#if this.isAuthenticating}}
  <AuLoader />
{{/if}}
{{#if this.errorMessage}}
  <AuAlert @alertIcon="cross" @alertTitle={{this.errorMessage}} @alertskin="error" />
{{/if}}
