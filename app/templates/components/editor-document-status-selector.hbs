<PowerSelect @loadingMessage="Aan het laden..." @noMatchesMessage="Geen resultaten" @searchMessage="Typ om te zoeken" @renderInPlace={{true}} @placeholder="Search by name" @allowClear={{allowClear}} @search={{perform search}} @selected={{document.status}} @onchange={{action "select"}} as |status|>
  {{status.name}}
</PowerSelect>
