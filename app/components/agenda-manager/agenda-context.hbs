{{yield (hash
                items=this.items
                loadItemsTask=this.loadItemsTask
                createItem=this.createItem
                deleteItem=this.deleteItem
                onSort=this.onSort
        )}}