<AuButton {{on "click" this.toggleModal}} @skin="secondary">
  {{t "voting.openModalButton"}}
</AuButton>

<Treatment::Voting::Modal
  @behandeling={{@behandeling}}
  @bestuursorgaan={{@bestuursorgaan}}
  @show={{this.showModal}}
  @onClose={{this.toggleModal}}
/>
