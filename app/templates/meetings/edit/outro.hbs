<AuModal @modalOpen={{true}}
         @size="fullscreen"
         @modalTitle={{t "meetingForm.sixthSectionTitle"}}
         @closeModal={{this.closeModal}}
         class="au-c-modal--editor"
         as |Modal|>
  <Modal.Body>
    <ZittingTextDocumentContainer
            @onEditorInit={{this.initEditor}}
            @saving={{this.saveTextTask.isRunning}}
            @type="ext:outro"
            @zitting={{@model}}/>
  </Modal.Body>
  <Modal.Footer>
    <AuButtonGroup>
      <AuButton {{on "click" (perform this.saveTextTask)}}
              @disabled={{this.saveTextTask.isRunning}}>
        {{t "meetings.edit.outro.save"}}
      </AuButton>
      <AuButton @disabled={{this.saveTextTask.isRunning}} {{on "click" this.saveAndQuit}}>
        {{t "meetings.edit.outro.saveAndQuit"}}
      </AuButton>
    </AuButtonGroup>
    <AuButtonGroup>
      <AuButton @skin="secondary"
                @disabled={{this.saveTextTask.isRunning}}
        {{on "click" this.cancel}}>
        {{t "meetings.edit.outro.cancel"}}
      </AuButton>
    </AuButtonGroup>
  </Modal.Footer>
</AuModal>
