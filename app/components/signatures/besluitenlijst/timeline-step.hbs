<Signatures::TimelineStep
  @name={{@name}}
  @document={{@lijst}}
  @errors={{@errors}}
  @signing={{@signing}}
  @publish={{@publish}}
  @mockDocument={{this.mockLijst}}
  @isOpen={{this.isOpen}}
/>
