<Signatures::TimelineStep 
  @name={{@name}} 
  @step={{@step}} 
  @document={{@notulen}} 
  @signing={{@signing}} 
  @publish={{@publish}} 
  @mockDocument={{this.mockNotulen}} 
  @isOpen={{this.isOpen}} 
>
  <div class="document-publish">
    {{{@zittingWrapper}}}
  </div>
  {{#if @zittingWrapper}}
    <EmberWormhole @to={{@behandelingContainerId}}>
      <Signatures::Notulen::BehandelingenList @notulen={{@notulen}} @prepublishedBehandelingen={{@behandelings}} @publicBehandelingUris={{@publicBehandelingUris}} @toggle={{action @togglePublicationStatus}} />
    </EmberWormhole>
  {{/if}}
</Signatures::TimelineStep>

