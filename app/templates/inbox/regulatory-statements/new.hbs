{{page-title (t 'inbox.regulatory-statements.new.title')}}
<DocumentCreator::Modal
  @modalTitle={{t 'inbox.regulatory-statements.new.title'}}
  @getTemplates={{this.getTemplates}}
  @onCancel={{this.cancelCreation}}
  @folderId={{this.folderId}}
  @onCreate={{this.redirectToStatement}}
/>