<label class="radio">
  <Input class="radio__toggle" @type="radio" @name={{@name}}
         checked={{eq @selectedOption @value}} @value={{@value}}
    {{on "click" (fn @selectOption @value)}}/>
  <span></span>
  {{yield}}
</label>
