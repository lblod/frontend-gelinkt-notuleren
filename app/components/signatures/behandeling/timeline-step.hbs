<Signatures::TimelineStep
  @name={{@name}}
  @document={{@behandeling}}
  @errors={{@errors}}
  @signing={{@signing}}
  @publish={{@publish}}
  @mockDocument={{this.mockBehandeling}}
  @isOpen={{this.isOpen}}
  @print={{@print}}
/>
