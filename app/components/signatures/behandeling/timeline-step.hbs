<Signatures::TimelineStep
  @name={{@name}}
  @document={{@behandeling}}
  @signing={{@signing}}
  @publish={{@publish}}
  @mockDocument={{this.mockBehandeling}}
  @isOpen={{this.isOpen}}
  @print={{@print}}
/>
