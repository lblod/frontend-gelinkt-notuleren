{{! @glint-nocheck }}
<AppChrome
  @editorDocument={{this.model.editorDocument}}
  @documentContainer={{this.model.documentContainer}}
>
  <:returnLink>
    <AgendapointBackLink />
  </:returnLink>
  <:actions>
    <AgendapointMenu @documentContainer={{this.model.documentContainer}} />
    <AuButton
      @disabled={{true}}
      @skin='naked'
      @icon='nav-up-down'
      @iconAlignment='right'
      class='au-u-padding-tiny'
    ><span class='au-u-h-functional au-u-regular'>{{t
          'utils.file-options'
        }}</span></AuButton>
    <AuButton @disabled={{true}}>{{t 'utils.save'}}</AuButton>
  </:actions>
</AppChrome>
<DocumentAttachments
  @documentContainer={{this.model.documentContainer}}
  @decisions={{this.model.decisions}}
/>