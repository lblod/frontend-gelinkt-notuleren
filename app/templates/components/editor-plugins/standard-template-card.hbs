<div class="modal-dialog modal-dialog--temporary-fix modal-dialog--sectioned modal-dialog--static modal-dialog--small">
	<div class="modal-dialog__header">
	  <div class="grid">
	    <div class="col--10-12">
	      <p>
					Wil u graag een sjabloon invoegen:
	      </p>
	    </div>
	{{!--     <div class="col--2-12 u-align-right">
	      <a class="link--icon--close" href="#">
	        <span class="u-visually-hidden">Venster sluiten</span>
	      </a>
	    </div> --}}
	  </div>
	</div>

	<div class="modal-dialog__link-list">
		<div class="grid">
			<div class="col--8-12">
				{{info.label}}
			</div>
			<div class="col--4-12 u-align-right">
			  {{wu-button
			    label="Voeg in"
			    isNarrow=true
			    onClick=(action "insert")}}
			</div>
		</div>
			{{!-- 	{{#wu-button-group}}
				  {{wu-button
				    label="Annuleer (todo)"
				    class="button--alt"
				    isNarrow=true
				    onClick=null}}
				  {{wu-button
				    label="Negeer suggestie (todo)"
				    class="button--link button--link--dark"
				    size="small"
				    isNarrow=true
				    onClick=null}}
				{{/wu-button-group}} --}}
	</div>
</div>

{{yield}}
