<EmberLoadRemover />

{{#let (config "environmentName") as |environmentName|}}
  {{page-title (concat
    "Gelinkt Notuleren"
    (if environmentName (concat " - " environmentName))
  )}}
{{/let}}

<AuApp>
  {{outlet}}

  <SnippetsNotifier />
</AuApp>
