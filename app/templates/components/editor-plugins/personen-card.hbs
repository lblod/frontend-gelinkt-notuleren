<div class="modal-dialog modal-dialog--small">
  <div class="modal-dialog__header">
    <div class="grid">
      <div class="col--10-12">
        <strong>Wil u {{info.person.fullName}} toevoegen?</strong>
      </div>
{{!--       <div class="col--2-12 u-align-right">
        <a class="link--icon--close" href="#">
          <span class="u-visually-hidden">Venster sluiten</span>
        </a>
      </div> --}}
    </div>
  </div>
  <div class="modal-dialog__content modal-dialog__content--sectioned">
    <ul>
      {{#each personCombinedWithProperties as |mp| }}
        <li class="u-hr u-padding--round--tiny">
          <a class="button button--narrow" onclick={{action 'refer' mp}}> Voeg toe als {{mp.prop.label}}</a>
          {{!-- <a class="button button--narrow button--link" onclick={{action 'extend' mp}}>Voeg uitgebreide gegevens toe</a> --}}
        </li>
      {{/each}}
    </ul>
  </div>
</div>
{{yield}}
