<AuModal @modalOpen={{true}}
         @size="fullscreen"
         @title={{t "meeting-form.sixth-section-title"}}
         @closeModal={{this.closeModal}}
         class="au-c-modal--editor"
         as |Modal|>
  <Modal.Body>
    <ZittingTextDocumentContainer
            @onEditorInit={{this.initEditor}}
            @saving={{this.saveTextTask.isRunning}}
            @type="ext:outro"
            @zitting={{@model}}/>
  </Modal.Body>
  <Modal.Footer>
    <AuButtonGroup>
      <AuButton @skin="naked"
                @disabled={{this.saveTextTask.isRunning}}
        {{on "click" this.closeModal}}>
        {{t "meetings.edit.outro.cancel"}}
      </AuButton>
    </AuButtonGroup>
    <AuButtonGroup>
      <AuButton @skin="secondary" {{on "click" (perform this.saveTextTask)}}
              @disabled={{this.saveTextTask.isRunning}}>
        {{t "meetings.edit.outro.save"}}
      </AuButton>
      <AuButton @disabled={{this.saveTextTask.isRunning}} {{on "click" this.saveAndQuit}}>
        {{t "meetings.edit.outro.save-and-quit"}}
      </AuButton>
    </AuButtonGroup>
  </Modal.Footer>
</AuModal>
<ConfirmRouteLeave @enabled={{this.dirty}} @message={{t "meetings.edit.outro.confirm-quit-without-saving"}}/>
