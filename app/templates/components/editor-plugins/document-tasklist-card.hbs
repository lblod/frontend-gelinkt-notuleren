<div class="modal-dialog__header">
  <div class="grid">
    <div class="col--10-12">
      <p class="u-spacer--tiny">
        Volgende takenlijst is beschikbaar voor dit document: <br>
        <strong>{{info.plainValue}}</strong>
      </p>
    </div>
    <div class="col--2-12 u-align-right">
      <a class="link--icon--close" href="#"> {{!-- TODO: action close {{action "remove" result.origModel}} --}}
        <span class="u-visually-hidden">Venster sluiten</span>
      </a>
    </div>
  </div>
</div>

<div class="modal-dialog__footer">
  {{#wu-button-group}}
    {{#wu-button isNarrow=true onClick=(action 'insert')}}{{info.plainValue}}{{/wu-button}}
    {{wu-button
      label="Annuleer (todo)"
      class="button--alt"
      isNarrow=true
      onClick=null}} {{!-- TODO: action close {{action "remove" result.origModel}} --}}
    {{wu-button
      label="Verwijder suggestie"
      class="button--link button--link--dark"
      size="small"
      isNarrow=true
       onClick=(action "remove")}}
  {{/wu-button-group}}
</div>

{{yield}}
